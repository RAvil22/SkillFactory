#pragma once
#include "user.h"
#include "chatExcept.h"

using namespace std;

/*class Client{
public:
    bool update(void);
    bool userEntry(unsigned int userID, string password);
private:

};*/
